version: 0.2

phases:
  install:
    commands:
      - echo "No dependencies required"
  build:
    commands:
      - echo "Building Static Website..."
  post_build:
    commands:
      - echo "Build Completed Successfully"

artifacts:
  files:
    - '**/*'
